logging.level.org.zalando.logbook: "INFO"
spring:
  sql:
    init:
      mode: "always"
  # в jdbc-url указано, что данные нужно сохранять в файл
  datasource:
    url: "jdbc:h2:file:./db/filmorate.mv.db"
    driverClassName: "org.h2.Driver"
    username: "sa"
    password: "password"
  h2:
    console:
      enabled: true